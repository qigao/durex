rootProject.name = 'Durex Template Projects'

apply from: "$rootDir/gradle/modules.gradle"

enableFeaturePreview("VERSION_CATALOGS")
dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("$rootDir/versions/libs.versions.toml"))
        }
        tlibs {
            from(files("$rootDir/versions/test.versions.toml"))
        }
        qlibs {
            from(files("$rootDir/versions/quarkus.versions.toml"))
        }
        slibs {
            from(files("$rootDir/versions/spring.versions.toml"))
        }
        glibs {
            from(files("$rootDir/versions/gradle.versions.toml"))
        }
    }
}

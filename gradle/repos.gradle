repositories{
    maven {
/*            credentials {
               username = "${repoUser}"
               password = "${repoPass}"
           } */
        url "https://maven.aliyun.com/repository/public"
    }
    gradlePluginPortal()
    maven { url 'https://maven-central.storage.googleapis.com' }
    mavenCentral()
}


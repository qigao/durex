test {
    useJUnitPlatform {
        excludeEngines 'junit-vintage'
    }
    testLogging {
        events "passed", "skipped", "failed"
        exceptionFormat 'full'
    }
    maxHeapSize = '1G'
    maxParallelForks=1
}

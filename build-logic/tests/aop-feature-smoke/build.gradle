plugins {
    id 'durex.spring-library'
}

import com.github.durex.gradle.model.DurexModuleModel

durex {
    aop()
}

assert extensions.getByType(DurexModuleModel).capabilities.get() == ['aop'] as Set
tasks.register('verifyAopCapability')

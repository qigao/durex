plugins { id 'durex.java-library' }

import com.github.durex.gradle.model.DurexModuleModel

durex {
    capability('durex.internal.fixture')
}

assert extensions.getByType(DurexModuleModel).capabilities.get().contains('fixture')
tasks.register('verifyGenericCapability')

pluginManagement {
    includeBuild('../..')
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
plugins {
    id 'durex.settings'
}

durexSettings {
    repositoryRoot.set(file('../../..'))
    moduleDiscovery.set(false)
}

rootProject.name = 'manifest-valid'

plugins {
    id 'io.github.qigao.simpledsl.build'
}

simpledsl {
    javaLibrary()
    lombok()
}

dependencies {
    implementation simpledsl.library('json-schema-pojo')
    implementation simpledsl.library('swagger-annotations')
}

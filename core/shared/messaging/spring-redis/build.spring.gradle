plugins {
    id 'io.github.qigao.simpledsl.build'
}

simpledsl {
    springLibrary()
    messaging()
    redis()
    aop()
    dependency('implementation', 'spring-json')
}

dependencies {
    api project(':messaging-api')
}

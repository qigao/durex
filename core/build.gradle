subprojects {
    apply plugin: 'io.freefair.lombok'
    if (path != ':core:music') {
        apply from: "$rootDir/gradle/extensions/jacoco.gradle"
    }
    apply from: "$rootDir/gradle/library/common.gradle"
}

plugins {
    alias glibs.plugins.sonarqube
    alias qlibs.plugins.quarkus
}

apply plugin: 'org.sonarqube'

sonarqube {
    properties {
        property "sonar.host.url", "https://sonarcloud.io"
        property "sonar.projectBaseDir", "$projectDir"
        property "sonar.projectKey", "com.xiavan.aihome:$project.name"
        property "sonar.projectName", "Quarkus Starter Project"
    }
}

jacocoTestReport {
    afterEvaluate {
        classDirectories.setFrom(files(classDirectories.files.collect {
            fileTree(dir: it, exclude: [
                '**/exceptions/**',
                '**/entity/**'
            ]
            )
        }))
    }
}
test {
    systemProperty "java.util.logging.manager", "org.jboss.logmanager.LogManager"
}

dependencies {
    implementation project(":core:shared:utils")
}

apply from: "$rootDir/gradle/library/quarkus.gradle"
apply from: "$rootDir/gradle/library/container.gradle"
